using System;
using System.Collections.Generic;
using System.Text;

namespace Business
{
    public static class QuickSort
    {
        public static List<int> GetLeftPivotRight(IReadOnlyCollection<int> arr)
        {
            var result = new List<int>();

            for (int i = 0; i < arr.Count; i++)
            {

            }

            return result;
        }
    }
}
